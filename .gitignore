# deps
node_modules/
.pnp
.pnp.*

# build
.next/
out/
build/
dist/

# env
.env
.env*.local

# сырые материалы заказчика (тяжёлые, не место в git)
assets/raw/

# vercel / typescript
.vercel
*.tsbuildinfo
next-env.d.ts

# os / editors
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# next / tooling (из create-next-app)
/coverage
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions
*.pem
.pnpm-debug.log*

# локальные инструменты Claude Code (сторонний скилл, не код проекта)
.claude/skills/
.claude/settings.local.json

# генерёнка медиа-конвейера (пересоздаётся из assets/raw)
assets/optimized/

# собранный файл дизайн-канваса — генерёнка редактора, пересобирается из .dc.html
docs/design/concepts/car-go-homepage-directions.html
